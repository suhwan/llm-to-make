# /check-tables - Airtable 테이블 구조 확인

이 스킬은 Airtable 테이블/필드 구조를 빠르게 확인합니다.

---

## 사용하는 MCP 도구

| 단계 | MCP 도구 | 용도 |
|------|----------|------|
| Step 1 | `mcp__airtable__list_tables` | 테이블 목록 조회 |
| Step 2 | `AskUserQuestion` | 조회 방식 선택 |
| Step 3 | `mcp__airtable__describe_table` | 테이블 상세 조회 |
| Step 4 | `AskUserQuestion` | 추가 조회 여부 |

---

## 실행 지침

### Step 1: 테이블 목록 조회

**mcp__airtable__list_tables 호출**:
```
baseId: appzQEgOxUpCYGmk7  (또는 .env의 AIRTABLE_BASE_ID)
detailLevel: "identifiersOnly"
```

**출력 형식**:
```
📊 Airtable Base: suwhan (appzQEgOxUpCYGmk7)

총 {N}개 테이블:

| # | 테이블명 | 테이블 ID |
|---|----------|------------|
| 1 | Models | tblOfVAOxlY29TBjH |
| 2 | Content_Requests | tblImBDg1HTY6sSsP |
| 3 | Generated_Contents | tblYEykfAmWSrtcP8 |
| ... | ... | ... |
```

---

### Step 2: 조회 방식 선택

**AskUserQuestion 사용**:
```yaml
question: "어떤 정보를 확인하시겠습니까?"
header: "조회 방식"
options:
  - label: "전체 요약 보기"
    description: "모든 테이블의 필드 수와 연결 관계를 간략히 표시"
  - label: "특정 테이블 상세"
    description: "선택한 테이블의 모든 필드 정보를 상세히 표시"
  - label: "테이블 관계도"
    description: "Linked Record 기반 테이블 간 관계를 시각화"
```

---

### Step 3-A: 전체 요약 (선택 시)

**모든 테이블에 대해 describe_table 호출** (detailLevel: "identifiersOnly"):

**출력 형식**:
```
📋 테이블 요약

┌─ Models ─────────────────────────────────────┐
│ 필드 수: 12개                                │
│ 주요 필드: Name, Main_Image, Status          │
│ 연결: → Content_Requests, Model_Personas     │
└──────────────────────────────────────────────┘

┌─ Content_Requests ───────────────────────────┐
│ 필드 수: 15개                                │
│ 주요 필드: Model_ID, Scene_Image, Status     │
│ 연결: ← Models | → Generated_Contents        │
└──────────────────────────────────────────────┘

┌─ Generated_Contents ─────────────────────────┐
│ 필드 수: 10개                                │
│ 주요 필드: Content_ID, Generated_Images      │
│ 연결: ← Content_Requests | → Generated_Video │
└──────────────────────────────────────────────┘
...
```

---

### Step 3-B: 특정 테이블 상세 (선택 시)

**먼저 테이블 선택 질문**:
```yaml
question: "어떤 테이블을 확인하시겠습니까?"
header: "테이블 선택"
options:
  # Step 1의 테이블 목록에서 동적으로 생성 (최대 4개)
  - label: "Models"
    description: "AI 모델 정보"
  - label: "Content_Requests"
    description: "콘텐츠 생성 요청"
  - label: "Generated_Contents"
    description: "생성된 이미지"
  # "Other" 옵션으로 나머지 테이블 입력 가능
```

**describe_table 호출** (detailLevel: "full"):
```
baseId: appzQEgOxUpCYGmk7
tableId: {선택한 테이블 ID}
detailLevel: "full"
```

**출력 형식**:
```
📋 {테이블명} 테이블 상세

테이블 ID: {테이블 ID}

┌──────────────────┬────────────────┬──────────────────────┐
│ 필드명           │ 타입           │ 옵션/연결            │
├──────────────────┼────────────────┼──────────────────────┤
│ {필드명}         │ {필드타입}     │ {옵션 또는 연결}     │
│ Content_ID       │ Auto Number    │ -                    │
│ Request_ID       │ Linked Record  │ → Content_Requests   │
│ Generated_Images │ Attachment     │ -                    │
│ Status           │ Single Select  │ 생성중, 완료, 실패   │
│ Created_At       │ Created Time   │ -                    │
└──────────────────┴────────────────┴──────────────────────┘

📎 연결된 테이블:
- {필드명} → {연결된 테이블명}
```

**필드 타입별 옵션 표시**:
- `Single Select` / `Multiple Select`: 선택지 나열
- `Linked Record`: 연결된 테이블명
- `Formula` / `Rollup`: 수식 또는 요약 방식
- 기타: `-` 표시

---

### Step 3-C: 테이블 관계도 (선택 시)

**모든 테이블의 Linked Record 필드 분석**:

1. 각 테이블의 describe_table 결과에서 `multipleRecordLinks` 타입 필드 추출
2. 연결 관계 매핑
3. ASCII 다이어그램 생성

**출력 형식**:
```
🔗 테이블 관계도

Models
  │
  ├──→ Content_Requests
  │         │
  │         └──→ Generated_Contents
  │                    │
  │                    └──→ Generated_Video_Contents
  │
  ├──→ Model_Personas
  │
  └──→ SNS_Posts

──────────────────────────────

독립 테이블 (연결 없음):
- Prompt_Templates
- Gen Models

──────────────────────────────

연결 상세:
| 출발 테이블 | 필드명 | 도착 테이블 |
|-------------|--------|-------------|
| Models | Content_Requests | Content_Requests |
| Models | Model_Personas | Model_Personas |
| Content_Requests | Model_ID | Models |
| Content_Requests | Generated_Contents | Generated_Contents |
| ... | ... | ... |
```

---

### Step 4: 추가 조회 여부

**AskUserQuestion 사용**:
```yaml
question: "다른 정보도 확인하시겠습니까?"
header: "추가 조회"
options:
  - label: "예, 다른 테이블 보기"
    description: "다른 테이블의 상세 정보를 확인합니다"
  - label: "예, 다른 방식으로 보기"
    description: "전체 요약/관계도 등 다른 방식으로 확인합니다"
  - label: "아니오, 종료"
    description: "테이블 확인을 마칩니다"
```

**분기**:
- "다른 테이블 보기" → Step 3-B로 (테이블 선택)
- "다른 방식으로 보기" → Step 2로 (조회 방식 선택)
- "아니오, 종료" → 종료 메시지

---

### 종료 메시지

```
✅ 테이블 확인 완료

💡 추가 명령어:
- /new-scenario : 시나리오 생성
- /status       : 프로젝트 현황
- "Models 테이블 필드 추가해줘" : 필드 직접 추가
```

---

## 빠른 사용법

사용자가 특정 테이블을 직접 언급한 경우 바로 해당 테이블 상세로 이동:

**예시**:
- "Generated_Contents 테이블 보여줘" → 바로 Step 3-B 실행
- "테이블 관계도 보여줘" → 바로 Step 3-C 실행
- "테이블 목록" → Step 1만 실행

---

## 주의사항

1. **Base ID 확인**: .env의 AIRTABLE_BASE_ID 사용
2. **API 호출 최적화**: 전체 요약 시 detailLevel을 "identifiersOnly"로 시작
3. **출력 정리**: 테이블/필드가 많을 경우 핵심 정보만 먼저 표시
4. **연결 분석**: Linked Record 필드의 linkedTableId로 관계 파악
